#we need to type-cast our string to an interger for the comparison operator >= to work
#for now just think of int() as a function that converts your string to interger
grade = int(input("what's your grade? "))
# we need to type-cast our string to an interger for the comparison operator >= to work
#for now just think of int() as a function that converts your string to an interger
# == eguality operator and it returns True if both values are the same and false otherwis
# + - * / are all operators
# <>= == are all comparison operators 
#for now just think of float
secretNum =67
if grade == 67:
    print("Ahhhhhh 676767676767676")
if grade >= 100: # exit condition - when do you stop your program?
    print("Beast mode.")
elif grade >= 90: # only runs if the previous condtion is false
    print("A game")
elif grade >= 80:# only runs if the previous condtion is false
    print("Beta Ohio")
elif grade >= 90:# only runs if the previous condtion is false
      print("A game.")
elif grade >= 80:# only runs if the previous condtion is false
      print("okay donkey")
elif grade < 70:# only runs if the previous condtion is false
      print("keep trying")

      